# Backend (FastAPI) configuration

# Job runner strategy: background | inline | celery
JOB_RUNNER=background

# Celery (optional; required only when JOB_RUNNER=celery)
#CELERY_BROKER_URL=redis://localhost:6379/0
#CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Storage backend (future: s3). Currently only 'local' is implemented
STORAGE_BACKEND=local

# Database URL (optional best-effort mirror). Example:
#DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

